<article>
  
  
</article>
<h1>Listado de roles</h1>

<p>
  {{link-to "Crear nuevo rol" "admin.roles.new" }}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th>
          Descripción
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each role in model}}
        <tr>
          <td>
            {{role.name}}
          </td>
          <td>
            {{role.description}}
          </td>
          <td>
            {{link-to "Editar" "admin.roles.edit" role}}
          </td>
          <td>
            <a href="#" {{action "remove" role}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
