import {
  linkSafe
} from '../../../helpers/link-safe';
import { module, test } from 'qunit';

module('LinkSafeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = linkSafe(42);
  assert.ok(result);
});
