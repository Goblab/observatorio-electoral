import {
  colorDiv
} from '../../../helpers/color-div';
import { module, test } from 'qunit';

module('ColorDivHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = colorDiv(42);
  assert.ok(result);
});
